version: '3.9'
services:
  api:
    build: .
    command: uvicorn backend.api:app --host 0.0.0.0 --port 3000
    ports:
      - "3000:3000"
    depends_on:
      - redis
      - db
  redis:
    image: redis:7
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: upos
      POSTGRES_PASSWORD: upos
      POSTGRES_DB: upos
    ports:
      - "5432:5432"
