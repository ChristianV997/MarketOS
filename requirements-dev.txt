# ── development / CI tooling ──────────────────────────────────────────────────
# Not required to run MarketOS — only for the advisory lint/coverage CI job
# (.github/workflows/ci.yml) and local development.
ruff==0.16.1
pytest-cov>=7.1.0
